document.title = 'Research - ' + document.title;

function showAbstract(id) {
  $("#" + id + " .abstract").css("display", "block");
  $("#" + id + " .add-info").css("display", "block");
}

function showBibtex(id) {
}

function showInfo(id) {
}
